<%- include("partials/top", { title }) %>

<section class="card narrow">
  <h1>Rejestracja</h1>
  <p>Konto jest aktywne od razu po utworzeniu.</p>

  <form class="form-stack" method="POST" action="/register">
    <label>
      Nazwa uzytkownika
      <input
        type="text"
        name="username"
        value="<%= formData?.username || '' %>"
        required
        minlength="3"
        maxlength="40"
        autocomplete="username"
      />
    </label>

    <label>
      Email
      <input
        type="email"
        name="email"
        value="<%= formData?.email || '' %>"
        required
        autocomplete="email"
      />
    </label>

    <label>
      Imie i nazwisko (opcjonalnie)
      <input
        type="text"
        name="name"
        value="<%= formData?.name || '' %>"
        autocomplete="name"
      />
    </label>

    <label>
      Haslo
      <input type="password" name="password" required autocomplete="new-password" />
    </label>

    <label>
      Powtorz haslo
      <input
        type="password"
        name="passwordConfirm"
        required
        autocomplete="new-password"
      />
    </label>

    <p class="muted">
      Haslo musi miec min. 10 znakow, mala i wielka litere, cyfre oraz znak
      specjalny.
    </p>

    <button class="btn btn--primary" type="submit">Utworz konto</button>
  </form>

  <p class="muted auth-switch">
    Masz juz konto? <a href="/login">Przejdz do logowania</a>
  </p>
</section>

<%- include("partials/bottom") %>
